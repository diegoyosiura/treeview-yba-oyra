{
  "compilerOptions": {
    "target": "ES5",
    "module": "CommonJS",
    "outDir": "./dist/cjs",
    "declaration": true,
    "declarationDir": "./dist/types",
    "strict": true,
    "lib": ["DOM", "ES6"],           // Usa apenas APIs do DOM e ES6
    "types": []                      // Não inclui automaticamente os tipos do Node.js
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
